# TODO

全书未决事项与待办的**唯一记录处**。定稿前删除本文件。

---

## A. 待拍板（会影响怎么写）

| # | 问题 | 谁在等它 |
|---|---|---|
| A1 | **结尾交付什么。** 要不要在最后一章交付一个命令行，例如 `toyllm "Once upon a time" --max-tokens 100 --temperature 0.8`，带 KV Cache、流式输出、EOS 停止 | 书名（「引擎」是否名副其实）、第十二章结尾 |
| A2 | **最终 demo 用 base 还是 -Instruct。** 135M base 只会续写不会对话，读者写完十二章输入「你好」得到一段胡编，落差较大。切 Instruct 要多讲 chat template，还要多下一份权重 | 第十章要不要讲 chat template、全书结尾的情绪落点 |
| A3 | **书名。** `_front.md` 已有英文副标题 *Build a Toy LLM Inference Engine (from Scratch)*，中文名仍待定 | 前言开头、封面 |
| A4 | **要不要「本书用到的 PyTorch」一节，放哪。** 读者画像写的是「可以不懂 PyTorch」，但第二章一上来就在讲 `nn.Linear` 的 `(out, in)` 布局 | 第二章开头没法定稿 |
| A5 | **环境搭建放哪**（装 uv、装 torch、下权重、代理坑）。`ch00.md` 并进前言后这块无家可归 | 候选：第一章开头 / 附录 A |
| A6 | **版本锁定怎么交代。** 读者用不同版本的 transformers，跑出来的数字和书里对不上。已决定「基准只存在代码里」，所以这条现在是 A5 的一部分，但仍需在某处告诉读者 | 同 A5 |
| A7 | **要不要附录，要哪几个。** 候选：A 环境与踩坑（代理、`torch<2.3`、`transformers<5` 的锁定原因）、B SmolLM2 config 速查表（几乎每章都在引用）、C 术语对照（可对齐 `../book` 附录 D） | |
| A8 | **图上标签用中文还是英文。** 正文中文，图上 Tokenization / Embedding Layer / Decoder Block 全英文。两种都行，但要统一 | 后面十几张机制图 |

---

## B. 结构冲突（插入第九章之后出现，必须解决）

| # | 问题 |
|---|---|
| B1 | **`ch05` 还有一整节 `## 残差连接`，和第九章重复。** 二选一：删掉 ch05 那节只留一句前向引用，或者把残差的基本概念留在 ch05、第九章只讲 HC/mHC/AttnRes |
| B2 | **`ch08` 仍宣称自己是「组装章」**（`本章代码` 块 + `## 接通：第一次完整前向`），但残差要到第九章才讲，没有残差就组不成 Block，第八章接不通。建议把「接通」整节搬到第九章 |

B1 和 B2 是同一个决定的两面，一起定。

---

## C. 待办（不需要拍板）

| # | 事情 | 备注 |
|---|---|---|
| C1 | **图还缺一批。** 三张最急：ch09 残差旁路图、ch11 prefill/decode 对比图、ch12 分块示意图（`../notes/2026-07-02-FA.md` 可复用）。另外 ch02–ch12 目前多数只借了第一章的定位图，没有机制图，粗估还要 15–20 张 | |
| C2 | **破折号待清，约 170 处。** 全在我写的占位文字里。不能机械替换成逗号，要逐句看。随各章改写时清理 | ch02–ch12、`_preface.md` |
| C3 | **各章「本章代码」脚手架块随章删除。** ch01 已删，其余十一章待删 | 已记入 `CLAUDE.md` |
| C4 | **前言四处人称换回作者原话。** 「AI 相关，必须，没啥可说的」「因为我的 MacBook 太老了」「我不想用 git 分支，感觉不好管理」「大概是这样，但需要边写边看」 | 现在是第三人称说明文 |
| C5 | **前言第 3 行断链。** TODO 写着「见『代码组织』」，但那一节不讲命令行交付（内容在本文件 A1） | 取决于 A1 |
| C6 | **前言风格未过。** 中英文之间还带空格，和正文（不加空格）不一致 | |
| C7 | **后记还没写，而且背着债。** 前言承诺了「然后再去学训练 / 再去学 GPU」，后记必须真给出这两条路 | |
| C8 | **致谢 TODO** | |
| C9 | **前言技术选型表里 Llama 3.2-1B / Qwen2.5-1.5B 的参数量待核实** | 定稿前查一次 |

---

## D. 已解决（留档，勿再提）

- 全书分两段：跑通（一到十章）/ 跑快（十一、十二章）
- 章名统一成「中文（English）」；`ch05` 二级标题改为「为什么需要归一化」
- `CLAUDE.md` 已按十二章重写，路径、命令、章节表均已更正
- 插入第九章后的 31 处交叉引用已全部对齐
- `top-k` / `top-p` 写法已统一（正文连字符、代码下划线）
- 权重共享的错误说法已更正：SmolLM2 三个尺寸**都**做共享，`tie_word_embeddings` 全为 `true`
- 基准输出只存在于代码中，正文不提；需要引用时说「第一章那句话」
- 不使用破折号，已记入 `CLAUDE.md`
